#include "lab2-api.h"
#include "usertraps.h"
#include "misc.h"

#include "spawn.h"

void main (int argc, char *argv[])
{
  
  lock_t lock1; //declare the lock
  lock_t lock2; //declare the lock
  lock_t lock3; //declare the lock
  lock_t lock4; //declare the lock
  lock_t lock5; //declare the lock
  lock_t lock6; //declare the lock
  lock_t lock7; //declare the lock
  lock_t lock8; //declare the lock
  lock_t lock9; //declare the lock
  lock_t lock10; //declare the lock
  lock_t lock11; //declare the lock
  lock_t lock12; //declare the lock
  lock_t lock13; //declare the lock
  lock_t lock14; //declare the lock
  lock_t lock15; //declare the lock
  lock_t lock16; //declare the lock
  lock_t lock17; //declare the lock
  lock_t lock18; //declare the lock
  lock_t lock19; //declare the lock
  lock_t lock20; //declare the lock
  lock_t lock21; //declare the lock
  lock_t lock22; //declare the lock
  lock_t lock23; //declare the lock
  lock_t lock24; //declare the lock
  lock_t lock25; //declare the lock
  lock_t lock26; //declare the lock
  lock_t lock27; //declare the lock
  lock_t lock28; //declare the lock
  lock_t lock29; //declare the lock
  lock_t lock30; //declare the lock
  lock_t lock31; //declare the lock
  lock_t lock32; //declare the lock
  lock_t lock33; //declare the lock

  cond_t cond1;
  cond_t cond2;
  cond_t cond3;
  cond_t cond4;
  cond_t cond5;
  cond_t cond6;
  cond_t cond7;
  cond_t cond8;
  cond_t cond9;
  cond_t cond10;
  cond_t cond11;
  cond_t cond12;
  cond_t cond13;
  cond_t cond14;
  cond_t cond15;
  cond_t cond16;
  cond_t cond17;
  cond_t cond18;
  cond_t cond19;
  cond_t cond20;
  cond_t cond21;
  cond_t cond22;
  cond_t cond23;
  cond_t cond24;
  cond_t cond25;
  cond_t cond26;
  cond_t cond27;
  cond_t cond28;
  cond_t cond29;
  cond_t cond30;
  cond_t cond31;
  cond_t cond32;
  cond_t cond33;

  lock1 = lock_create();
  //Printf("%d\n", lock1);
  lock2 = lock_create();
  lock3 = lock_create();
  lock4 = lock_create();
  lock5 = lock_create();
  lock6 = lock_create();
  lock7 = lock_create();
  lock8 = lock_create();
  lock9 = lock_create();
  lock10 = lock_create();
  lock11 = lock_create();
  lock12 = lock_create();
  lock13 = lock_create();
  lock14 = lock_create();
  lock15 = lock_create();
  lock16 = lock_create();
  lock17 = lock_create();
  lock18 = lock_create();
  lock19 = lock_create();
  lock20 = lock_create();
  lock21 = lock_create();
  lock22 = lock_create();
  lock23 = lock_create();
  lock24 = lock_create();
  lock25 = lock_create();
  lock26 = lock_create();
  lock27 = lock_create();
  lock28 = lock_create();
  lock29 = lock_create();
  lock30 = lock_create();
  lock31 = lock_create();
  lock32 = lock_create();
  lock33 = lock_create();

  cond1 = cond_create(lock1);
  cond2 = cond_create(lock2);
  cond3 = cond_create(lock3);
  cond4 = cond_create(lock4);
  cond5 = cond_create(lock5);
   cond6 = cond_create(lock6);
   cond7 = cond_create(lock7);
   cond8 = cond_create(lock8);
   cond9 = cond_create(lock9);
   cond10 = cond_create(lock10);
   cond11 = cond_create(lock11);
   cond12 = cond_create(lock12);
   cond13 = cond_create(lock13);
   cond14 = cond_create(lock14);
   cond15 = cond_create(lock15);
   cond16 = cond_create(lock16);
   cond17 = cond_create(lock17);
   cond18 = cond_create(lock18);
   cond19 = cond_create(lock19);
   cond20 = cond_create(lock20);
   cond21 = cond_create(lock21);
   cond22= cond_create(lock22);
   cond23= cond_create(lock23);
   cond24= cond_create(lock24);
   cond25= cond_create(lock25);
   cond26= cond_create(lock26);
   cond27= cond_create(lock27);
   cond28= cond_create(lock28);
   cond29= cond_create(lock29);
   cond30= cond_create(lock30);
   cond31= cond_create(lock31);
   cond32= cond_create(lock32);
   cond33= cond_create(lock33);

}
